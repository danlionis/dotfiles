#!/bin/bash

if (($# == 0)); then
    echo "Usage: $0 [window-pattern] [url-and-flags...]"
    exit 1
fi

WINDOW_PATTERN="$1"
shift
LAUNCH_COMMAND="lio-launch-webapp $@"

exec lio-launch-or-focus "$WINDOW_PATTERN" "$LAUNCH_COMMAND"
