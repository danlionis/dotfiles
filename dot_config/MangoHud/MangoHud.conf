# === FPS & FRAMETIMES ===
fps
fps_limit=0
frametime
frame_timing
histogram
fps_color_change
fps_value=60,80
fps_color=FF4040,FFAA00,00FF00

# === GPU ===
gpu_stats
gpu_temp
gpu_junction_temp          # Critical for 7800 XT — watch for >110°C
gpu_mem_temp
gpu_power
gpu_load_change
throttling_status          # Will explicitly flag if throttling occurs
vram
gpu_name
gpu_voltage

# === CPU ===
cpu_stats
cpu_temp
cpu_power
cpu_mhz
cpu_load_change

# === RAM ===
ram
swap

# === SYSTEM ===
io_read
io_write
arch
wine
vulkan_driver              # Confirms RADV version

# === GRAPH ===
gpu_temp_value=75,95
cpu_temp_value=75,95

# === FRAME TIMING GRAPH ===
frame_timing_detailed=1

